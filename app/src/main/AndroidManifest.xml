<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lucky.dev.tu.devandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".Login"></activity>-->
        <activity
            android:name=".Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" /></intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Resum"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".Wishlish"></activity>
        <activity android:name=".ProducBrandtActivity"></activity>
        <activity android:name=".ProductDetail"></activity>
        <activity android:name=".Search"></activity>
    </application>

</manifest>